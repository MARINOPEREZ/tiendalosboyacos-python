<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@android:drawable/sym_def_app_icon"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:theme="@android:style/Theme.Material.Light"
        android:usesCleartextTraffic="true">

        <!--
          usesCleartextTraffic="true": el backend Flask corre en HTTP
          plano en la red local (XAMPP, sin certificado). Desde API 28
          Android bloquea HTTP por defecto; esto lo habilita solo para
          este entorno de desarrollo/pruebas academico.
        -->

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.CatalogoActivity" android:exported="false" />
        <activity android:name=".ui.DetalleActivity" android:exported="false" />

    </application>

</manifest>
